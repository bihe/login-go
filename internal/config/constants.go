package config

// FlashKeyError is used as a flash message for errors
const FlashKeyError = "flash_error"

// FlashKeyInfo is used as a key for flash messages
const FlashKeyInfo = "flash_info"

// User defines an authenticated user object stored in the context
const User = "context_user"

// ErrorPath defines the URL-path for errors
const ErrorPath = "error"

// CookieDefaultExp is the default expiry time of a cookie
const CookieDefaultExp = 60
